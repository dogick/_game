#pragma once
static const int WINDOW_WIDTH = 1300;
static const int WINDOW_HEIGHT = 700;
static const int PLAYER_SPEED = 100;
static const int ENEMY_SPEED = 60;
static const float PI = 3.14159265f;
static const sf::Time TIME_PER_FRAME = sf::seconds(1.f / 60.f);
static const  sf::Vector2f PLAYER_SIZE(40.f, 40.f);
static const  sf::Vector2f ENEMY_SIZE(40.f, 40.f);
static const  sf::Vector2f AREA_SIZE(400.f, 400.f);